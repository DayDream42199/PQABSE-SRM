Persistent cache for file-count scaling experiments.

The `generated/` subdirectory is created by `run_file_count_experiments.py`
and stores reusable encrypted bundle corpora so future sessions do not need to
re-encrypt the same max-size dataset again.

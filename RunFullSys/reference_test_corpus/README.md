Persistent cache for `run_reference_paper_experiments.py`.

The `generated/` subdirectory stores reusable Paper 1 corpora so file-scaling
and keyword-scaling benchmark runs can reuse previously generated synthetic
corpora instead of rebuilding them every session.
